<!DOCTYPE html>
<html>
  <head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">

    <title>ArService</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    
  </head>

  <body>
    <header>
      <div class="d-flex bd-highlight align-items-center bgc-black">
        <div class="p-2 bd-highlight">
          <a href="/users">
            <img src="https://presite-potepancamp-rails-02.herokuapp.com/assets/logo.png" alt="サイトのロゴ画像">
          </a>
        </div>
        <div class="p-2 bd-highlight">
          <%= form_tag search_path, method: :get do %>
            <%= text_field_tag :search_area, params[:search_area], placeholder: "エリアを入力", class: "index-h" %>
          <% end %>
        </div>
        <div class="p-2 bd-highlight">
          <%= form_tag search_path, method: :get do %>
            <%= text_field_tag :search, params[:search], placeholder: "キーワードを入力", class: "index-h" %>
          <% end %>
        </div>
  
        <% if logged_in? %>
          <%= render 'users/logged_in' %>
        <% else %>
          <%= render 'users/not_logged_in' %>
        <% end %>
      </div>
    </header>

    <%= yield %>

    <footer>
      <p>© 2020 POTEPAN.INC.</p>
    </footer>
    
  </body>
</html>